# news-admin backend 本地开发环境变量模板：复制为 backend/.env 并按需修改。
# 生产环境通过真实环境变量注入，无需 .env 文件。

# 部署环境：development | production
APP_ENV=development

# HTTP 监听端口
APP_PORT=8080

# 日志级别：debug | info | warn | error
LOG_LEVEL=info

# 允许跨域访问的来源（逗号分隔，如本地 Vite dev server）
CORS_ORIGIN=http://localhost:5173